[build]
builder = "NIXPACKS"
buildCommand = "echo 'worker deps installed via nixpacks'"

[deploy]
startCommand = "npm start --workspace=mindsprint-worker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
