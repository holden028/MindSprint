[build]
builder = "NIXPACKS"
buildCommand = "echo 'api deps installed via nixpacks'"

[deploy]
startCommand = "npm start --workspace=mindsprint-api"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 300
