[build]
builder = "NIXPACKS"
buildCommand = "npm run build --workspace=mindsprint-web"

[deploy]
startCommand = "npm start --workspace=mindsprint-web"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
